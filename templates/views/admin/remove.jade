extends ../../layouts/default

block content
    .container
        h1 Remove from Organization
        if error 
            p Oops, looks like there was an error removing #{ghUser} from #{org}.
            pre= error
        else
            p You have removed #{ghUser} from #{org}.